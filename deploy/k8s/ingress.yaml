apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: klimatsearch
  labels:
    app: klimatsearch
  annotations:
    # stub: replace with a real ingress class in production
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: klimatsearch
                port:
                  name: http
